
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MoBoo App</title>
    <link rel="stylesheet" href="styles/main.css">
</head>
<body>
    <h1>MoBoo Library<img src="/public/images/moboologo.png"/></h1>
    <div class="container"> 
        <div>
            <a class="" href="/movies">
                Movies
            </a>
            <% for (let movie of movies) { %>
                <div>
                <h1> <%=movie.title %> </h1>
                <a href="/movies/show/<%= movie._id %>">
                <img alt="<%= movie.description%>" src="<%=movie.img%>" 
                id="movies" height="500"/>
                </a>
                
                </div>
            <%}%>
            
        </div>  
        <div> 
            <a class="" href="/books">
              Books
            </a>
            <% for (let book of books) { %>
                <div>
                <h1> <%=book.title %> </h1>
                <a href="/books/show/<%= book._id %>">
                <img alt="<%= book.author%>" src="<%=book.img%>" 
                id="book" height="500"/>
                </div>
             <%}%>
            
        </div>
    </div>
</body>
</html>