<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MoBoo Library</title>
</head>
<body>
    Hello 
    World
    <% for (let movie of movies) { %>
        <div>
        <h1> <%=movie.name %> </h1>
        <img alt="<%= movie.name%>" src="<%=movie.img%>" id="poster-image">
        </div>
    <%}%>
</body>
</html>